import { ID } from "../../../../../../_metronic/helpers";

export type Stats = {
    id?: ID,
    name?: string,
    email?: string,
    phone?: string,
    cleanerprice?: string,
    packageprice?: string,
    discountprice?: string,
    gst?: string,
    total?: string,
}